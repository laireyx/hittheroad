import { style } from '@vanilla-extract/css';

export const transparentBorderStyle = style({
  border: '1px solid transparent',
});

export const roundBorderStyle = style({
  borderRadius: '8px',
});
